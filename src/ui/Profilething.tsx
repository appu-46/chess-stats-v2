import styled from 'styled-components'

const StyledProfilething = styled.div`
  display: grid;
  grid-template-columns: auto 1fr;
  grid-template-rows: auto auto auto;
  align-items: center;
  gap: 2rem;
  // min-height: 15rem;
`

export default StyledProfilething
